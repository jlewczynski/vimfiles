 Installation:

    git clone git://github.com/jlewczynski/dotvim.git ~/.vim

Create symlinks:

    ln -s ~/.vim/vimrc ~/.vimrc
    ln -s ~/.vim/gvimrc ~/.gvimrc

Get plugins:
    cd ~/.vim
    git submodule init
    git submodule update
